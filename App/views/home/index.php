<div class="container">
        <div class="content">
            <nav>
                <a href="<?php echo PROJECT_PATH. 'login'; ?>">Login</a> |
                <a href="<?php echo PROJECT_PATH. 'home/register'; ?>">Register</a>
            </nav>

            <?php
                //var_dump($this);
            ?>
            <div class="right-column">
                <section class="categories">
                    <ul>--Categories--
                        <li>Country</li>
                        <li>Genre</li>
                        <li>Top Downloads</li>

                    </ul>
                </section>
            </div>
            
            <div class="left-column">
                <div class="content-body">
                
                section Well variables inside namespaces do not override others since variables are never affected by namespace but always global:"Although any valid PHP code can be contained within a namespace, only four types of code are affected by namespaces: classes, interfaces, functions and constants. "
            section Well variables inside namespaces do not override others since variablessection Well variables inside namespaces do not override others since variables are never affected by namespace but always global:"Although any valid PHP code can be contained within a namespace, only four types of code are affected by namespaces: classes, interfaces, functions and constants. "
            section Well variables inside namespaces do not override others since variablessection Well variables inside namespaces do not override others since variables are never affected by namespace but always global:"Although any valid PHP code can be contained within a namespace, only four types of code are affected by namespaces: classes, interfaces, functions and constants. "
            section Well variables inside namespaces do not override others since variablessection Well variables inside namespaces do not override others since variables are never affected by namespace but always global:"Although any valid PHP code can be contained within a namespace, only four types of code are affected by namespaces: classes, interfaces, functions and constants. "
            section Well variables inside namespaces do not override others since variablessection Well variables inside namespaces do not override others since variables are never affected by namespace but always global:"Although any valid PHP code can be contained within a namespace, only four types of code are affected by namespaces: classes, interfaces, functions and constants. "
            section Well variables inside namespaces do not override others since variablesvsection Well variables inside namespaces do not override others since variables are never affected by namespace but always global:"Although any valid PHP code can be contained within a namespace, only four types of code are affected by namespaces: classes, interfaces, functions and constants. "
            section Well variables inside namespaces do not override others since variablessection Well variables inside namespaces do not override others since variables are never affected by namespace but always global:"Although any valid PHP code can be contained within a namespace, only four types of code are affected by namespaces: classes, interfaces, functions and constants. "
            section Well variables inside namespaces do not override others since variablessection Well variables inside namespaces do not override others since variables are never affected by namespace but always global:"Although any valid PHP code can be contained within a namespace, only four types of code are affected by namespaces: classes, interfaces, functions and constants. "
            section Well variables inside namespaces do not override others since variablessection Well variables inside namespaces do not override others since variables are never affected by namespace but always global:"Although any valid PHP code can be contained within a namespace, only four types of code are affected by namespaces: classes, interfaces, functions and constants. "
            section Well variables inside namespaces do not override others since variablessection Well variables inside namespaces do not override others since variables are never affected by namespace but always global:"Although any valid PHP code can be contained within a namespace, only four types of code are affected by namespaces: classes, interfaces, functions and constants. "
            section Well variables inside namespaces do not override others since variablessection Well variables inside namespaces do not override others since variables are never affected by namespace but always global:"Although any valid PHP code can be contained within a namespace, only four types of code are affected by namespaces: classes, interfaces, functions and constants. "
            section Well variables inside namespaces do not override others since variablessection Well variables inside namespaces do not override others since variables are never affected by namespace but always global:"Although any valid PHP code can be contained within a namespace, only four types of code are affected by namespaces: classes, interfaces, functions and constants. "
            section Well variables inside namespaces do not override others since variablessection Well variables inside namespaces do not override others since variables are

                </div>
            </div>
        </div>
</div>
